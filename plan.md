# SKALA Vue 날씨 과제 검증 계획

> 작성 기준: 참조된 이전 대화 전체와 현재 요청을 대조했다. 이전 대화에는 과제 세부 원문이 추가로 남아 있지 않았으므로, 아래에는 현재 요청에서 확인되는 요구사항만 기록한다. 제공되지 않은 세부 요구사항은 임의로 만들지 않고 `원문 미확인`으로 표시한다.

## 검증 상태 표기

- `[ ]` 미검증 또는 미구현
- `[x]` 코드에서 구현 근거 확인
- 각 항목 아래에 구현 파일/근거와 판정을 기록한다.

## 과제 1~5 파일 분리 필수 요건

- [x] 과제 1 결과 파일이 다른 단계와 구분되어 별도로 존재한다. — `WeatherMockup.vue`, `/practice1`
- [x] 과제 2 결과 파일이 다른 단계와 구분되어 별도로 존재한다. — `WeatherComposition.vue`, `/practice2`
- [x] 과제 3 결과 파일이 다른 단계와 구분되어 별도로 존재한다. — `WeatherParent.vue`, `/practice3`
- [x] 과제 4 결과 파일이 다른 단계와 구분되어 별도로 존재한다. — `WeatherHomeView.vue`, `/practice4`
- [x] 과제 5 결과 파일이 다른 단계와 구분되어 별도로 존재한다. — `PracticeFiveView.vue`, `/practice5` 및 `/`
- [x] 각 결과 파일에서 해당 단계의 필수 기능과 개인 Customization을 독립적으로 확인할 수 있다.
- [x] 각 결과 파일이 단순 백업본이 아니라 해당 단계 결과물로 식별 가능한 이름과 내용을 갖는다.
- [x] 분석 시 과제 1~5 각각의 대응 파일 경로, 실행 연결 여부, 구현 근거를 별도로 기록한다. — `README.md` URL·파일 표

## 최종 요구 구조 유지 원칙 (교재 PDF 178쪽 — 사용자 제공 내용 기준)

> 과제 1~5를 분리하더라도 아래 최종 구조와 각 파일의 역할을 훼손하지 않는다. 별도의 `src/assignments` 트리로 최종 앱을 이중화하지 않고, 단계별 결과물을 기존 Vue 구조 안에서 식별 가능하게 정리한다.

```text
src/
├── main.js                       # Router 인스턴스 전역 주입: .use(router)
├── App.vue                       # <RouterLink> 내비게이션과 <RouterView /> 배치
├── router/
│   └── index.js                  # routes 배열, Lazy Loading, Catch-all 정의
├── components/
│   └── exercise/
│       ├── BaseDashboardCard.vue
│       ├── SearchBar.vue
│       └── WeatherCard.vue
└── views/
    ├── WeatherHomeView.vue
    ├── WeatherAboutView.vue
    ├── WeatherDetailView.vue
    └── NotFoundView.vue
```

- [x] `src/main.js`가 최종 진입점이며 Router를 `.use(router)`로 주입한다.
- [x] `src/App.vue`에 `<RouterLink>` 내비게이션과 `<RouterView />`가 존재한다.
- [x] `src/router/index.js`가 실제 실행 앱에서 사용하는 Router 설정 파일이다.
- [x] `src/components/exercise/`에 실습용 재사용 부품을 격리한다.
- [x] `src/views/`에 Home, About, 동적 Detail, NotFound 페이지를 유지한다.
- [x] 과제별 분리를 위해 최종 구조의 필수 파일을 삭제·이름 변경·우회하지 않는다.
- [x] `.exercise`, `.before*`, `.after*`, `.autoGenerated` 파일은 제출 결과물의 대체물이 아니라 참고 이력으로만 취급한다.

## 과제 1~5 단계별 파일 배치 방향

### 과제 1 — Weather Mockup

- [x] `src/components/exercise/WeatherMockup.vue`를 과제 1의 독립 결과 파일로 식별한다.
- [x] Mock 배열, `v-for`, `:key="id"`, 25도 기준 `v-if`/`v-else`, 과제 1 개인 Customization을 이 파일에서 확인할 수 있다.
- [x] 최종 서비스 전체 기능을 과제 1의 추가 요구사항으로 간주하지 않는다.

### 과제 2 — Composition API

- [x] `src/components/exercise/WeatherComposition.vue`를 과제 2의 독립 결과 파일로 식별한다.
- [x] 과제 1 결과를 바탕으로 Composition API 요구사항과 과제 2 개인 Customization이 명확히 드러난다.
- [x] 과제 2와 무관한 최종 서비스 기능을 추가 구현하지 않는다.

### 과제 3 — Components

- [x] `src/components/exercise/WeatherParent.vue`를 과제 3의 조립 화면으로 식별한다.
- [x] `BaseDashboardCard.vue`, `SearchBar.vue`, `WeatherCard.vue`를 과제 3의 분리된 부품으로 사용한다.
- [x] 부모/자식 데이터 및 이벤트 흐름과 과제 3 개인 Customization을 독립적으로 확인할 수 있다. — 추가 컴포넌트 `WeatherStatusBar.vue`

### 과제 4 — Router

- [x] 최종 요구 구조의 `main.js`, `App.vue`, `router/index.js`, `views/`를 과제 4 결과물로 사용한다.
- [x] `/`, `/about`, `/weather/:cityId` 및 Catch-all 경로를 실제 Router에 연결한다.
- [x] 요구되는 라우트 컴포넌트에 Lazy Loading을 적용한다.
- [x] 과제 3의 `components/exercise/` 부품을 `WeatherHomeView.vue`에서 재사용한다.
- [x] 과제 4 개인 Customization을 Router 구조를 훼손하지 않는 범위에서 식별한다. — `/practice1`~`/practice5` 평가 URL

### 과제 5 — Pinia Store

- [x] 과제 4의 Router 기반 최종 구조를 그대로 유지하면서 Pinia를 누적 적용한다.
- [x] `src/stores/configStore.js`를 추가 구조로 허용하되 기존 필수 경로의 역할을 변경하지 않는다.
- [x] `main.js`에서 Router와 Pinia를 함께 전역 주입한다.
- [x] `unit: 'celsius'`, `unitSymbol`, `toggleUnit`을 실제 화면 컴포넌트와 연결한다.
- [x] 과제 5 개인 Customization을 Store 또는 단위 전환 UI에서 독립적으로 확인할 수 있다. — `UnitToggler.vue`

## 작업 경계

- [x] 완성형 서비스를 `WeatherHomeView.vue`로 이전하고 `WeatherMockup.vue`는 과제 1 전용으로 정리했다.
- [x] 새로운 서비스 기능 개발보다 기존 코드에서 과제별 요구 근거를 분리·정리하는 작업을 우선했다.
- [x] 단계별 파일을 정리한 뒤에도 최종 앱은 위 Router 중심 구조로 실행된다.
- [x] 사용자 승인 후 코드 이동·이름 변경·연결 작업을 수행했다.

## 1단계 — Weather Mockup

- [x] 임의 날씨 데이터가 배열로 정의되어 있다. — `WeatherMockup.vue`
- [x] 날씨 데이터 배열을 `v-for`로 반복 렌더링한다.
- [x] 반복 렌더링 요소에 `:key="id"`를 사용한다.
- [x] `v-if`를 사용해 온도가 25도 이상이면 `더움 (25도 이상)`을 표시한다.
- [x] 25도 미만인 경우를 조건부로 구분해 표시한다.
- [x] 이 단계에 개인 Customization이 적용되어 있다. — 카드 grid, 날씨 아이콘, 반응형 UI
  - 세부 지정 내용: 원문 미확인. 코드에서 과제 기본 요건을 넘어선 개인화 요소가 있는지 확인하되, 특정 기능을 필수로 간주하지 않는다.

## 2단계 — Composition API

- [x] Composition API 단계 요구사항이 구현되어 있다. — `ref`, `computed`, `watch`, `watchEffect`
- [x] 이 단계에 개인 Customization이 적용되어 있다. — 실시간 도시 검색과 선택 상태 표시

## 3단계 — Components

- [x] Components 단계 요구사항이 구현되어 있다. — 부모 반응형 데이터, Base slot, SearchBar·WeatherCard props/emits, 컴포넌트별 scoped style
- [x] 이 단계에 개인 Customization이 적용되어 있다. — `WeatherStatusBar.vue` 추가 분리

## 4단계 — Router

- [x] 라우팅 요구사항이 구현되어 있다. — `/`, `/about`, `/weather/:cityId`, `/practice1`~`/practice5`
- [x] 라우트 컴포넌트에 Lazy Loading이 적용되어 있다.
- [x] Catch-all 라우트와 404 화면 처리가 구현되어 있다.
- [x] 이 단계에 개인 Customization이 적용되어 있다. — 단계별 평가 URL과 17개 지역 상세 화면

## 5단계 — Pinia Store

- [x] Pinia Store가 구성되어 있다. — `src/stores/configStore.js`
- [x] state에 `unit`이 있고 초기값이 `celsius`이다.
- [x] getter `unitSymbol`이 구현되어 있다.
- [x] action `toggleUnit`이 구현되어 있다.
- [x] 이 단계에 개인 Customization이 적용되어 있다. — 완성형 Home과 상세 화면의 섭씨/화씨 표시 연동

## 6단계 — Axios

- [ ] Axios 단계 요구사항이 구현되어 있다.
  - API 종류, 엔드포인트, 호출 방식 및 예외 처리의 세부 지정 내용: 원문 미확인. 의존성과 실제 호출 코드를 조사해 기록한다.
- [ ] 이 단계에 개인 Customization이 적용되어 있다.
  - 세부 지정 내용: 원문 미확인. 특정 기능을 임의로 필수화하지 않는다.

## 7단계 — UI Library

- [ ] UI Library 단계 요구사항이 구현되어 있다.
  - 지정 라이브러리와 필수 UI 요소: 원문 미확인. 의존성과 실제 사용 코드를 조사해 기록한다.
- [ ] 이 단계에 개인 Customization이 적용되어 있다.
  - 세부 지정 내용: 원문 미확인. 특정 라이브러리나 기능을 임의로 필수화하지 않는다.

## 8단계 — Refinement

- [ ] Refinement 단계 요구사항이 구현되어 있다.
  - 성능, 접근성, 반응형 UI, 오류/로딩 처리 등 구체 기준: 원문 미확인. 코드에서 확인되는 개선 사항만 근거와 함께 기록한다.
- [ ] 이 단계에 개인 Customization이 적용되어 있다.
  - 세부 지정 내용: 원문 미확인. 특정 개선을 임의로 필수화하지 않는다.

## 9단계 — Deployment

- [ ] Deployment 단계 요구사항이 구현되어 있다.
  - 배포 플랫폼, 빌드 설정, 제출 URL 등 구체 기준: 원문 미확인. 저장소에 있는 설정과 문서만 조사해 기록한다.
- [ ] 이 단계에 개인 Customization이 적용되어 있다.
  - 세부 지정 내용: 원문 미확인. 특정 배포 기능을 임의로 필수화하지 않는다.

## 공통 제출사항

- [ ] 공통 제출사항의 원문과 제출 형식을 확인했다.
  - 세부 지정 내용: 참조된 이전 대화와 현재 요청에 원문이 없어 미확인. 임의의 제출물, URL, 캡처, 문서 형식을 필수로 추가하지 않는다.
- [x] 확인 가능한 제출 관련 파일·문서·설정을 찾아 근거를 기록했다. — `README.md`, `vercel.json`, `.env.example`, `SECURITY.md`
- [x] 1~9단계 각각의 구현 여부, 개인 Customization 여부, 근거 파일을 본 문서에 정리했다. — 6~9는 원문 미확인 상태 유지

## 분석 순서

- [x] 프로젝트 디렉터리 구조 확인
- [x] `package.json`의 의존성 및 스크립트 확인
- [x] `src` 핵심 파일 확인
- [x] 1~9단계 및 공통 제출사항별 구현 근거와 누락 사항 정리
- [x] 과제 1~5 구현 후 빌드·정적 검사 결과 보고
